# Venus

Application PHP de navigation et d'administration des rubriques CRAc-RF.

## Configuration locale

La configuration locale se fait dans `config/config.php`, a partir du modele `config/config.example.php`.

## Base de donnees

Le schema de base vide est disponible dans `database/schema.sql`.

Aucune donnee applicative n'est versionnee dans le depot.
